/*package org.generation.test;

import static org.junit.Assert.assertFalse;
import static org.junit.jupiter.api.Assertions.assertTrue;

import org.generation.persona.Persona;
import org.junit.jupiter.api.Test;

public class PersonaTest {
Requerimientos:
 * 
 * 1. Instanciar 4 objetos con los parámetros establecidos en el constructor
 * 
 * 2. Crear métodos(4)para probar cada objeto con los valores de los atribut 	(getters)
 * 
 * 3. Crear un método de validación email, donde el email debe cumplir con la validación @gmail.com ... si cumple con la condición, el test se evalúa correcto, si no, se evalua como falla (método de test booleano, regex)
 * 
 * 
 * 
 * 

	 @Test
	    public void testValidarEmail() {
	        // Crear un objeto Persona con un email válido
	        Persona personaValida = new Persona("Cindy", "Rejon", "cindy@gmail.com", "32");
	        assertTrue(personaValida.validarEmail());

	        // Crear un objeto Persona con un email no válido
	        Persona personaNoValida = new Persona("Mon", "Martinez", "mon@gmail.com", "23");
	        assertFalse(personaNoValida.validarEmail());
	    }
*/
	
	
	
	
}
